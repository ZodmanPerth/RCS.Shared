<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:ice="http://schemas.microsoft.com/winfx/2006/xaml/presentation/options"
	xmlns:system="clr-namespace:System;assembly=mscorlib"
>

	<ResourceDictionary.MergedDictionaries>
		<ResourceDictionary Source="pack://application:,,,/OKB.Theme;component/Colours/SystemColours.xaml" />
		<ResourceDictionary Source="pack://application:,,,/OKB.Theme;component/Controls/ScrollViewer/Base.ScrollViewer.xaml" />
	</ResourceDictionary.MergedDictionaries>


	<!--Core Colours from aero2-->


	<Color x:Key="Colour.List.Border">#828790</Color>

	<Color x:Key="Colour.ListViewItem.FocusVisual.Border">#8E6EA6F5</Color>

	<Color x:Key="Colour.ListViewItem.Background.Hover">#FFD5F1FE</Color>
	<Color x:Key="Colour.ListViewItem.Background.Selected">#FF9BDDFB</Color>
	<Color x:Key="Colour.ListViewItem.Background.Selected.Hover">#FFC9EDFD</Color>
	<Color x:Key="Colour.ListViewItem.Background.Selected.Inactive">#FFDDDDDD</Color>

	<Color x:Key="Colour.ListViewItem.Border.Hover">#FFCCF0FF</Color>
	<Color x:Key="Colour.ListViewItem.Border.Selected">#FF98DDFB</Color>
	<Color x:Key="Colour.ListViewItem.Border.Selected.Hover">#FF98DDFB</Color>
	<Color x:Key="Colour.ListViewItem.Border.Selected.Inactive">#FFCFCFCF</Color>



	<!--Base references-->


	<system:Double x:Key="ListViewItem.FocusVisual.BorderThickness">1</system:Double>
	<system:Double x:Key="ListViewItem.FocusVisual.Radius.X">2</system:Double>
	<system:Double x:Key="ListViewItem.FocusVisual.Radius.Y">2</system:Double>

	<HorizontalAlignment x:Key="ListViewItem.HorizontalContentAlignment">Left</HorizontalAlignment>
	<VerticalAlignment x:Key="ListViewItem.VerticalContentAlignment">Center</VerticalAlignment>
	<Thickness x:Key="ListViewItem.Margin">0,0,0,1</Thickness>
	<Thickness x:Key="ListViewItem.Padding">5,2</Thickness>
	<Thickness x:Key="ListViewItem.BorderThickness">1</Thickness>
	<CornerRadius x:Key="ListViewItem.CornerRadius">2</CornerRadius>


	<SolidColorBrush x:Key="Brush.ListViewItem.FocusVisual.Border"				Color="{StaticResource Colour.ListViewItem.FocusVisual.Border}"				ice:Freeze="true" />


	<SolidColorBrush x:Key="Brush.ListView.Foreground"							Color="{StaticResource SystemColours.ControlTextBrush}"						ice:Freeze="true" />

	<SolidColorBrush x:Key="Brush.ListView.Background"							Color="{StaticResource SystemColours.WindowBrush}"							ice:Freeze="true" />
	<SolidColorBrush x:Key="Brush.ListView.Background.Disabled"					Color="{StaticResource SystemColours.ControlBrush}"							ice:Freeze="true" />

	<SolidColorBrush x:Key="Brush.ListView.Border"								Color="{StaticResource Colour.List.Border}"									ice:Freeze="true" />
	<SolidColorBrush x:Key="Brush.ListView.Border.Disabled"						Color="{StaticResource SystemColours.ControlBrush}"							ice:Freeze="true" />


	<SolidColorBrush x:Key="Brush.ListViewItem.Foreground.Disabled"				Color="{StaticResource SystemColours.GrayTextBrush}"						ice:Freeze="true" />

	<SolidColorBrush x:Key="Brush.ListViewItem.Background"						Color="Transparent"														ice:Freeze="true" />
	<SolidColorBrush x:Key="Brush.ListViewItem.Background.Hover"				Color="{StaticResource Colour.ListViewItem.Background.Hover}"				ice:Freeze="true" />
	<SolidColorBrush x:Key="Brush.ListViewItem.Background.Selected"				Color="{StaticResource Colour.ListViewItem.Background.Selected}"			ice:Freeze="true" />
	<SolidColorBrush x:Key="Brush.ListViewItem.Background.Selected.Hover"		Color="{StaticResource Colour.ListViewItem.Background.Selected.Hover}"		ice:Freeze="true" />
	<SolidColorBrush x:Key="Brush.ListViewItem.Background.Selected.Inactive"	Color="{StaticResource Colour.ListViewItem.Background.Selected.Inactive}"	ice:Freeze="true" />

	<SolidColorBrush x:Key="Brush.ListViewItem.Border"							Color="Transparent"														ice:Freeze="true" />
	<SolidColorBrush x:Key="Brush.ListViewItem.Border.Hover"					Color="{StaticResource Colour.ListViewItem.Border.Hover}"					ice:Freeze="true" />
	<SolidColorBrush x:Key="Brush.ListViewItem.Border.Selected"					Color="{StaticResource Colour.ListViewItem.Border.Selected}"				ice:Freeze="true" />
	<SolidColorBrush x:Key="Brush.ListViewItem.Border.Selected.Hover"			Color="{StaticResource Colour.ListViewItem.Border.Selected.Hover}"			ice:Freeze="true" />
	<SolidColorBrush x:Key="Brush.ListViewItem.Border.Selected.Inactive"		Color="{StaticResource Colour.ListViewItem.Border.Selected.Inactive}"		ice:Freeze="true" />



	<!-- ======== FocusVisual ======== -->

	<ControlTemplate x:Key="Base.ControlTemplate.ListViewItem.FocusVisual" >
		<Rectangle
			StrokeThickness="1"
			RadiusX="2"
			RadiusY="2"
			Stroke="{DynamicResource Brush.ListViewItem.FocusVisual.Border}"
		/>
	</ControlTemplate>

	<Style x:Key="Base.Style.ListViewItem.FocusVisual" >
		<Setter Property="Control.Template" Value="{DynamicResource Base.ControlTemplate.ListViewItem.FocusVisual}" />
	</Style>


	<!-- ======== ListViewItem ======== -->


	<Style x:Key="Base.Style.ListViewItem" TargetType="ListViewItem">
		<Setter Property="HorizontalContentAlignment" Value="{DynamicResource ListViewItem.HorizontalContentAlignment}"/>
		<Setter Property="VerticalContentAlignment" Value="{DynamicResource ListViewItem.VerticalContentAlignment}"/>
		<Setter Property="Margin" Value="{DynamicResource ListViewItem.Margin}"/>
		<Setter Property="Padding" Value="{DynamicResource ListViewItem.Padding}"/>
		<Setter Property="BorderThickness" Value="{DynamicResource ListViewItem.BorderThickness}"/>
		<Setter Property="Background" Value="{DynamicResource Brush.ListViewItem.Background}"/>
		<Setter Property="BorderBrush" Value="{DynamicResource Brush.ListViewItem.Border}"/>
		<Setter Property="FocusVisualStyle" Value="{DynamicResource Base.Style.ListViewItem.FocusVisual}"/>
		<Setter Property="SnapsToDevicePixels" Value="True"/>
		<Setter Property="Template">
			<Setter.Value>
				<ControlTemplate TargetType="ListViewItem">
					<Border
						BorderThickness="{TemplateBinding BorderThickness}"
						BorderBrush="{TemplateBinding BorderBrush}"
						Background="{TemplateBinding Background}"
						CornerRadius="{DynamicResource ListViewItem.CornerRadius}"
						SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"
					>
						<ContentPresenter
							HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
							VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
							SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"
						/>
					</Border>
					<ControlTemplate.Triggers>
						<Trigger Property="IsMouseOver" Value="true" >
							<Setter Property="Background" Value="{DynamicResource Brush.ListViewItem.Background.Hover}" />
							<Setter Property="BorderBrush" Value="{DynamicResource Brush.ListViewItem.Border.Hover}"/>
						</Trigger>
						<Trigger Property="IsSelected" Value="true">
							<Setter Property="Background" Value="{DynamicResource Brush.ListViewItem.Background.Selected}"/>
							<Setter Property="BorderBrush" Value="{DynamicResource Brush.ListViewItem.Border.Selected}"/>
						</Trigger>
						<MultiTrigger>
							<MultiTrigger.Conditions>
								<Condition Property="IsSelected" Value="true"/>
								<Condition Property="Selector.IsSelectionActive" Value="false"/>
							</MultiTrigger.Conditions>
							<Setter Property="Background" Value="{DynamicResource Brush.ListViewItem.Background.Selected.Inactive}"/>
							<Setter Property="BorderBrush" Value="{DynamicResource Brush.ListViewItem.Border.Selected.Inactive}"/>
						</MultiTrigger>
						<MultiTrigger>
							<MultiTrigger.Conditions>
								<Condition Property="IsSelected" Value="true"/>
								<Condition Property="IsMouseOver" Value="true"/>
							</MultiTrigger.Conditions>
							<Setter Property="Background" Value="{DynamicResource Brush.ListViewItem.Background.Selected.Hover}"/>
							<Setter Property="BorderBrush" Value="{DynamicResource Brush.ListViewItem.Border.Selected.Hover}"/>
						</MultiTrigger>
						<Trigger Property="IsEnabled" Value="false">
							<Setter Property="Foreground" Value="{DynamicResource Brush.ListViewItem.Foreground.Disabled}"/>
						</Trigger>
					</ControlTemplate.Triggers>
				</ControlTemplate>
			</Setter.Value>
		</Setter>
	</Style>



	<!-- ======== ListView ======== -->


	<ControlTemplate x:Key="Base.ControlTemplate.ListView" TargetType="ListView">
		<Border
			x:Name="Bd"
			BorderThickness="{TemplateBinding BorderThickness}"
			BorderBrush="{TemplateBinding BorderBrush}"
			Background="{TemplateBinding Background}"
			SnapsToDevicePixels="true"
		>
			<ScrollViewer
				Focusable="false"
				Padding="{TemplateBinding Padding}"
			>
				<ItemsPresenter SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"/>
			</ScrollViewer>
		</Border>
		<ControlTemplate.Triggers>
			<Trigger Property="IsEnabled" Value="false">
				<Setter Property="Background" TargetName="Bd" Value="{DynamicResource Brush.ListView.Background.Disabled}"/>
				<Setter Property="BorderBrush" TargetName="Bd" Value="{DynamicResource Brush.ListView.Border.Disabled}"/>
			</Trigger>
			<MultiTrigger>
				<MultiTrigger.Conditions>
					<Condition Property="IsGrouping" Value="true"/>
					<Condition Property="VirtualizingPanel.IsVirtualizingWhenGrouping" Value="false"/>
				</MultiTrigger.Conditions>
				<Setter Property="ScrollViewer.CanContentScroll" Value="false"/>
			</MultiTrigger>
		</ControlTemplate.Triggers>
	</ControlTemplate>

	<Style x:Key="Base.Style.ListView" TargetType="ListView">
		<Setter Property="Foreground" Value="{DynamicResource Brush.ListView.Foreground}"/>
		<Setter Property="Background" Value="{DynamicResource Brush.ListView.Background}"/>
		<Setter Property="BorderBrush" Value="{DynamicResource Brush.ListView.Border}"/>
		<Setter Property="BorderThickness" Value="1"/>
		<Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Auto"/>
		<Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Auto"/>
		<Setter Property="ScrollViewer.CanContentScroll" Value="true"/>
		<Setter Property="ScrollViewer.PanningMode" Value="Both"/>
		<Setter Property="Stylus.IsFlicksEnabled" Value="False"/>
		<Setter Property="VerticalContentAlignment" Value="Center"/>
		<Setter Property="ItemContainerStyle" Value="{DynamicResource Base.Style.ListViewItem}"/>
		<Setter Property="Template" Value="{DynamicResource Base.ControlTemplate.ListView}"/>
		<Setter Property="IsTabStop" Value="False"/>
		<Setter Property="Focusable" Value="False"/>

	</Style>

</ResourceDictionary>
